#include <iostream>
#include <cmath>
using namespace std;
int main(){
    double a, b, c;
    cout<<"Введите a: ";
    cin>>a;
    cout<<"Введите b: ";
    cin>>b;
    c=sqrt(pow(a, 2)+pow(b, 2));
    cout<<"Гипотенуза = "<<c<<endl;
    return 0;
}
