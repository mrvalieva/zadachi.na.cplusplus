#include <iostream>
using namespace std;

int main() {
    for (int num = 120; num <= 140; num++) {
        int count = 0;
        for (int i = 1; i <= num; i++) {
            if (num % i == 0) {
                count++;
            }
        }
        cout << "Количество делителей числа " << num << ": " << count << endl;
    }

    return 0;
}
