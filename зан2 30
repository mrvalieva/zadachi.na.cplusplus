#include <iostream>
using namespace std;

int main() {
    int n;
    cout << "Введите целое число: ";
    cin >> n;

    if (n > 0) {
        n += 1;
    } else if (n < 0) {
        n -= 2;
    } else {
        n = 10;
    }

    cout << "Результат: " << n;

    return 0;
}
