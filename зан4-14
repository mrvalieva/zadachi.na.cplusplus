#include <iostream>
using namespace std;

int main() {
    for (int num = 100; num <= 300; num++) {
        int sum = 0;
        for (int i = 1; i <= num; i++) {
            if (num % i == 0) {
                sum += i;
            }
        }
        if (sum == 50) {
            cout << num << " ";
        }
    }

    return 0;
}
