#include <iostream>
using namespace std;

int main() {
    for (int num = 300; num <= 600; num++) {
        int sum = 0;
        for (int i = 1; i <= num; i++) {
            if (num % i == 0) {
                sum += i;
            }
        }
        if (sum % 10 == 0) {
            cout << num << " ";
        }
    }

    return 0;
}
